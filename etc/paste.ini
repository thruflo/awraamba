[composite:main]
use = egg:Paste#urlmap
/ = dynamic
/static = static
/thumbnails = thumbnails

[app:dynamic]
use = egg:awraamba#main

cookie_secret = secret

login_url = /login
logout_url = /logout

template_directories = [%(here)s/../src/awraamba/templates]
static_files_path = %(here)s/../build/static
thumbnails_directory = %(here)s/../var/thumbnails
locale_directory = %(here)s/../build/i18n
default_language = en
supported_languages = [en]

db = postgresql
db_name = awraamba
db_user = dev
db_password = password
db_host = localhost
db_port = 5432
pg_catalogs = english

site_title = Awra Amba Experience
google_analytics = 'UA-...'

site_email_address = thruflo+awraamba@gmail.com
smtp_domain = smtp.gmail.com
smtp_username = sidf@green.tv
smtp_password = triumph4nt

dev = true
log_level = info

[app:static]
use = egg:Paste#static
document_root = %(here)s/../build/static

[app:thumbnails]
use = egg:Paste#static
document_root = %(here)s/../var/thumbnails

[server:main]
use = egg:PasteScript#wsgiutils
host = 127.0.0.1
port = 8080
